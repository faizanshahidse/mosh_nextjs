"use client";

import React from "react";

const AddToCartButton = () => {
  return (
    <button onClick={() => console.log("added in cart...")}>Add to Cart</button>
  );
};

export default AddToCartButton;
